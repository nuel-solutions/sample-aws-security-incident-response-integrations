[tool:pytest]
markers =
    requires_slack_sdk: marks tests as requiring Slack SDK dependencies (slack-bolt, slack-sdk)
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests